/** @type {import('next').NextConfig} */
const nextConfig = {
  // NOTE: the previous `/' → '/login' redirect was removed — the public
  // SCEM institutional portal now serves at '/' (EduPulse login lives in
  // the slide-in drawer and at '/login').
};

export default nextConfig;
